<%= form_with(model: football_player) do |form| %>
  <% if football_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(football_player.errors.count, "error") %> prohibited this football_player from being saved:</h2>

      <ul>
        <% football_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
